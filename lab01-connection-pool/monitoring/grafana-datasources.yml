# monitoring/grafana-datasources.yml
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    uid: prometheus  # ← CRITICAL: Must match dashboard references!
    jsonData:
      timeInterval: "5s"
